import(parallel)
import(locfit)
importFrom(coda, mcmc)
importFrom(Rmpi, mpi.finalize)
importFrom(degreenet, dwar)

useDynLib(size)

importFrom(utils, packageDescription)

# Export all names that do not begin with a dot.
exportPattern("^[^\\.]")
